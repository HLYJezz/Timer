/*
 * ────────────────────────────────────────────────────────────────
 *  แก้ไฟล์นี้ไฟล์เดียว แล้วแอปจะซิงก์ข้ามเครื่องผ่าน Firebase
 *  (ดูขั้นตอนละเอียดใน README หัวข้อ "ขึ้น GitHub Pages")
 * ────────────────────────────────────────────────────────────────
 *
 *  ถ้าปล่อย databaseURL ว่างไว้ แอปจะไม่ยุ่งกับ Firebase เลย
 *  และจะใช้ server.js บน Wi-Fi วงเดียวกันแทน (โหมดเดิม)
 */

window.FIREBASE_CONFIG = {
  apiKey: '',
  authDomain: '',
  databaseURL: '', // ← ตัวสำคัญ เช่น https://xxx-default-rtdb.asia-southeast1.firebasedatabase.app
  projectId: '',
  appId: ''
};

/*
 *  ROOM_ID = ห้องของค่ายคุณ
 *
 *  ใครก็ตามที่รู้ค่านี้ (และรู้ลิงก์เว็บ) กดปุ่มได้หมด
 *  → ตั้งเป็นอะไรที่เดาไม่ถูก อย่าใช้คำง่าย ๆ อย่าง "camp"
 *  เช่น 'medcamp-2569-x7k2vq'
 */
window.ROOM_ID = 'เปลี่ยนค่านี้ด้วย-อะไรก็ได้ที่เดายาก';

/*
 *  เวอร์ชัน Firebase SDK ที่จะโหลด (ปกติไม่ต้องแก้)
 */
window.FIREBASE_SDK_VERSION = '10.12.2';
